# ===========================================
# ScoreIndia Production Environment Variables
# Domain: scoreindia.cloud
# ===========================================
# Copy this to .env on your VPS and fill in the values

# ==== Database ====
# Generate: openssl rand -base64 32
DB_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# ==== JWT Authentication ====
# CRITICAL: Generate a strong 64+ character secret
# Command: openssl rand -base64 64
JWT_SECRET=REPLACE_WITH_64_CHAR_SECRET
JWT_EXPIRY_MINUTES=15
REFRESH_EXPIRY_HOURS=168

# ==== Environment ====
ENVIRONMENT=production

# ==== CORS (Backend) ====
ALLOWED_ORIGINS=https://scoreindia.cloud,https://www.scoreindia.cloud

# ==== Frontend URLs ====
# Update these based on your actual setup:
# Option 1: If API is on subdomain (recommended)
NEXT_PUBLIC_API_URL=https://api.scoreindia.cloud/api
NEXT_PUBLIC_WS_URL=wss://api.scoreindia.cloud

# Option 2: If API is on same domain with path
# NEXT_PUBLIC_API_URL=https://scoreindia.cloud/api
# NEXT_PUBLIC_WS_URL=wss://scoreindia.cloud

# ==== AWS S3 (for player image uploads) ====
AWS_ACCESS_KEY=
AWS_SECRET_KEY=
S3_BUCKET=scoreindia-storage
S3_REGION=ap-south-1

# ==== Server ====
PORT=3001
