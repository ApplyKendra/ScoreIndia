# Database
DB_PASSWORD=your-secure-database-password

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long
JWT_EXPIRY_MINUTES=15
REFRESH_EXPIRY_HOURS=168

# CORS
ALLOWED_ORIGINS=http://localhost:3000

# AWS S3 (for image uploads)
AWS_ACCESS_KEY=
AWS_SECRET_KEY=
S3_BUCKET=your-bucket-name
S3_REGION=ap-south-1

# Server
PORT=3001

# AWS SSM Parameter Store (optional - for secure credential storage)
# When enabled, secrets are fetched from SSM with fallback to env vars
USE_AWS_SSM=false
AWS_REGION=ap-south-1
SSM_PARAMETER_PREFIX=/auctionapp/development
# Note: AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY should be set for SSM access
# Alternatively, use IAM roles when deployed on AWS infrastructure
